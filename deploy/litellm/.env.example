# LiteLLM Proxy 专用：厂商真密钥只配这里（复制为同目录 .env）
# 业务根目录 .env 里的 LLM_API_KEY 必须与本文件 LITELLM_MASTER_KEY 相同

# Proxy 对外那一把 sk（自己起一个 sk- 开头的即可）
LITELLM_MASTER_KEY=sk-litellm-master

# 下游厂商真实 API Key（按你启用的模型填写）
OPENAI_API_KEY=sk-你的openai真key
# ANTHROPIC_API_KEY=sk-ant-...

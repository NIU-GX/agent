[project]
name = "agent-eval"
version = "0.1.0"
description = "Retrieval, generation heuristic, and trajectory eval"
requires-python = ">=3.12"
dependencies = [
  "agent-shared",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["eval"]
